Instructions
============

* Delete existing dotfiles at `~/.vimrc`, `~/.vim` and `~/.tmux.conf`.
* Install Vim 8+ and Python 3 manually if `apt-get` is not supported.
* Run setup script: `bash setup.sh`
